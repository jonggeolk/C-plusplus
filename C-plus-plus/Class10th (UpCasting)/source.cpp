#include <iostream>
#include "../Program/Zuggling.h"
#include "../Program/Hydra.h"
#include "../Program/Lurker.h"
#include "../Program/Beacon.h"

using namespace std;

int main()
{
#pragma region 업 캐스팅
	//Beacon beacon;

	//Zerg* zuggling = new Zuggling();
	//Zerg* hydra = new Hydra();
	//Zerg* lurker = new Lurker();

	//zuggling->SetHP(10);
	//hydra->SetHP(15);
	//lurker->SetHP(20);

	//cout << "zuggling의 현재체력 : " << zuggling->GetHP() << endl;
	//cout << "hydra의 현재체력 : " << hydra->GetHP() << endl;
	//cout << "lurker의 현재체력 : " << lurker->GetHP() << endl;


	//beacon.Trigger(zuggling);
	//beacon.Trigger(hydra);
	//beacon.Trigger(lurker);

	//cout << "zuggling의 현재체력 : " << zuggling->GetHP() << endl;
	//cout << "hydra의 현재체력 : " << hydra->GetHP() << endl;
	//cout << "lurker의 현재체력 : " << lurker->GetHP() << endl;


#pragma endregion


	return 0;
}
